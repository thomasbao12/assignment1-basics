Hyperparameter search order:
learning_rate
beta2
weight_decay
beta1

I then added learning rate cosine scheduling but used hyperparameters from my initial sweep with a constant learning rate

Additional Sweeps

learning_rate_with_scheduling
batch_size
